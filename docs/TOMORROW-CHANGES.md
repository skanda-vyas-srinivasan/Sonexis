# Sonexis — Tomorrow’s Change List

## Confirmed changes

- [x] **Make recording global/combined.** The Record button captures the combined processed output from all active chains in one recording session and one output file.
- [ ] **Fix Add App hover in the menu bar.** Add App should reliably show a pink underline on hover without flickering the app rows or dismissing the menu before an app can be added.
- [ ] **Remove hover-subtitle/dialogue overlays.** Eliminate any temporary hover subtitles, instructional dialogue, or hover copy that can appear unexpectedly in the UI. Keep necessary warnings and confirmations.
- [ ] **Make “Unlink preset” more noticeable.** Improve its discoverability in the preset context menu while keeping the existing unlink behavior and Sonexis theme.

## Possible follow-ups to consider

- [ ] Lock chain selection while a recording is active, or clearly show which chains are included in the combined recording.
- [ ] Verify the 24 FPS flow default and the hidden Flow control in a fresh launch.
- [ ] Verify starter-preset migration on an existing install: the nine `Bina’s …` presets plus `MightBeTooMuch` should appear once.
- [ ] Do a non-full-screen visual pass for menu-bar sizing, scrolling after four chains, and app-name truncation.
- [ ] Decide whether the menu-bar chain removal action should remain immediate with no confirmation.
